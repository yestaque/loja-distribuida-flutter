name: flutter_layout
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.1.0
  url_launcher: ^6.2.5
  flutter_launcher_icons: ^0.13.1
  geolocator: 14.0.2
  qr_flutter: ^4.1.0
  provider: ^6.1.2
  firebase_core: ^3.8.0
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.4.0
  google_sign_in: ^7.2.0
  dio: ^5.9.2
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/img/

flutter_icons:
  android: true
  ios: true
  image_path: "assets/img/woman-clothes.png"
